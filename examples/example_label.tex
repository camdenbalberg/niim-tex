\documentclass[10pt]{article}
\usepackage[paperwidth=50mm,paperheight=12mm,margin=0mm]{geometry}
\usepackage{tikz}
\usepackage[T1]{fontenc}
\usepackage{lmodern}
\pagestyle{empty}
\topskip=0pt
\parindent=0pt

\begin{document}
\noindent
\begin{tikzpicture}[x=1mm,y=1mm]
  \useasboundingbox (0,0) rectangle (50,12);
  % Example label for niim-tex
  %
  % This label works on any label at least 12mm wide and 50mm long.
  % The D110 printhead is 12mm, so the printable area is always
  % 12mm x <length>mm regardless of tape width.
  %
  % Compatible sizes: 15x50, 14x60, 12.5x74, or any custom label
  % where tape width >= 12mm and length >= 50mm.
  %
  % Print:  python niim_tex.py print examples/example_label.tex

  % Black accent bar on the left
  \fill[black] (0,0) rectangle (2.5,12);

  % Thin accent line near the bar
  \fill[black] (3.2,1.2) rectangle (3.5,10.8);

  % Main text
  \node[anchor=west, font=\fontsize{11}{13}\selectfont\bfseries\sffamily]
    at (5,7.5) {CAMDEN};

  % Subtitle with letterspacing
  \node[anchor=west, font=\fontsize{4.5}{5}\selectfont\sffamily, text=black!70]
    at (5.2,3.5) {E\kern0.8pt N\kern0.8pt G\kern0.8pt I\kern0.8pt N\kern0.8pt E\kern0.8pt E\kern0.8pt R\kern0.8pt I\kern0.8pt N\kern0.8pt G};

  % Decorative dot cluster
  \fill[black] (44,9.5) circle (0.35);
  \fill[black] (46,9.5) circle (0.35);
  \fill[black] (48,9.5) circle (0.35);

  % Bottom accent line
  \fill[black] (5,1.8) rectangle (48.5,2.1);
\end{tikzpicture}
\end{document}
